SUPABASE_PROJECT_ID=""
SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_URL=""
# Service role key (SECRETO · salta RLS). Necesaria para backup admin y /verificar/<código>.
# Obténla en: Supabase Dashboard → Settings → API → service_role. NUNCA subir el valor a git.
SUPABASE_SERVICE_ROLE_KEY=""
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""
# Base URL of the app (no trailing slash). Used for QR codes in PDFs, og:url meta tags, and the sitemap.
VITE_APP_URL=""